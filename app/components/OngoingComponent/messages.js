/*
 * OngoingComponent Messages
 *
 * This contains all the text for the OngoingComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OngoingComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OngoingComponent component!',
  },
});
