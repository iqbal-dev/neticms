/*
 * BreadcrumComponent Messages
 *
 * This contains all the text for the BreadcrumComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BreadcrumComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BreadcrumComponent component!',
  },
});
