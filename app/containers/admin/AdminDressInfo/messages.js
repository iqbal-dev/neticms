/*
 * AdminDressInfo Messages
 *
 * This contains all the text for the AdminDressInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminDressInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminDressInfo container!',
  },
});
