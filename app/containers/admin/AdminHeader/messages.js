/*
 * AdminHeader Messages
 *
 * This contains all the text for the AdminHeader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminHeader container!',
  },
});
