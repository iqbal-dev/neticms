// import { fromJS } from 'immutable';
// import { selectAdminDownloadCornerDomain } from '../selectors';

describe('selectAdminDownloadCornerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
