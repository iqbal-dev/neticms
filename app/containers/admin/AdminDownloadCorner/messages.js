/*
 * AdminDownloadCorner Messages
 *
 * This contains all the text for the AdminDownloadCorner container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminDownloadCorner';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminDownloadCorner container!',
  },
});
