/*
 * AppPrivateLayout Messages
 *
 * This contains all the text for the AppPrivateLayout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppPrivateLayout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppPrivateLayout container!',
  },
});
