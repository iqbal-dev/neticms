/*
 * AdminHomepage Messages
 *
 * This contains all the text for the AdminHomepage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminHomepage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminHomepage container!',
  },
});
