/*
 * AdminSideBar Messages
 *
 * This contains all the text for the AdminSideBar container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminSideBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminSideBar container!',
  },
});
