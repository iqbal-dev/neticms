/*
 * AdminFeesInfo Messages
 *
 * This contains all the text for the AdminFeesInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminFeesInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminFeesInfo container!',
  },
});
