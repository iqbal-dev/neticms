/*
 * AllNotice Messages
 *
 * This contains all the text for the AllNotice container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllNotice';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllNotice container!',
  },
});
