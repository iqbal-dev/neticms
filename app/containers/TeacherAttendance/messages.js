/*
 * TeacherAttendance Messages
 *
 * This contains all the text for the TeacherAttendance container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeacherAttendance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeacherAttendance container!',
  },
});
