// import { fromJS } from 'immutable';
// import { selectSectionWiseAttendanceDomain } from '../selectors';

describe('selectSectionWiseAttendanceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
