/*
 * SectionWiseAttendance Messages
 *
 * This contains all the text for the SectionWiseAttendance container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SectionWiseAttendance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SectionWiseAttendance container!',
  },
});
