// import { fromJS } from 'immutable';
// import { selectStudentWiseAttendanceDomain } from '../selectors';

describe('selectStudentWiseAttendanceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
