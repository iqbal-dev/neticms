/*
 * StudentWiseAttendance Messages
 *
 * This contains all the text for the StudentWiseAttendance container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentWiseAttendance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentWiseAttendance container!',
  },
});
