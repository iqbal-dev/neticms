/*
 * MeritList Messages
 *
 * This contains all the text for the MeritList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MeritList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MeritList container!',
  },
});
