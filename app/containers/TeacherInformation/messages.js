/*
 * TeacherInformation Messages
 *
 * This contains all the text for the TeacherInformation container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeacherInformation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeacherInformation container!',
  },
});
