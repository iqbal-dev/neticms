/*
 * OnlineClassRoutine Messages
 *
 * This contains all the text for the OnlineClassRoutine container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OnlineClassRoutine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OnlineClassRoutine container!',
  },
});
