/*
 * FailList Messages
 *
 * This contains all the text for the FailList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FailList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FailList container!',
  },
});
