/*
 * CommitteeMembers Messages
 *
 * This contains all the text for the CommitteeMembers container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommitteeMembers';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommitteeMembers container!',
  },
});
