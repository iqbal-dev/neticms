/*
 * StuffInformation Messages
 *
 * This contains all the text for the StuffInformation container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StuffInformation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StuffInformation container!',
  },
});
