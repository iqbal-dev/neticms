/*
 * DressCode Messages
 *
 * This contains all the text for the DressCode container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DressCode';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DressCode container!',
  },
});
