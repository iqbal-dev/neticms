/*
 * EventGallery Messages
 *
 * This contains all the text for the EventGallery container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EventGallery';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EventGallery container!',
  },
});
