/*
 * TopStudents Messages
 *
 * This contains all the text for the TopStudents container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TopStudents';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopStudents container!',
  },
});
