/*
 * FeesInfo Messages
 *
 * This contains all the text for the FeesInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeesInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeesInfo container!',
  },
});
