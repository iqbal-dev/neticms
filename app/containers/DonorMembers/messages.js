/*
 * DonorMembers Messages
 *
 * This contains all the text for the DonorMembers container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DonorMembers';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DonorMembers container!',
  },
});
