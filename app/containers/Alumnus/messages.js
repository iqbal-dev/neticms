/*
 * Alumnus Messages
 *
 * This contains all the text for the Alumnus container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Alumnus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Alumnus container!',
  },
});
