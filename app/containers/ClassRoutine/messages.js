/*
 * ClassRoutine Messages
 *
 * This contains all the text for the ClassRoutine container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClassRoutine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClassRoutine container!',
  },
});
