/*
 * SyllabusInfo Messages
 *
 * This contains all the text for the SyllabusInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SyllabusInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SyllabusInfo container!',
  },
});
