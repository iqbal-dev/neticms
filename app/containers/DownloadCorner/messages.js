/*
 * DownloadCorner Messages
 *
 * This contains all the text for the DownloadCorner container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DownloadCorner';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownloadCorner container!',
  },
});
