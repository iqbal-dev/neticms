/*
 * OnlineAdmission Messages
 *
 * This contains all the text for the OnlineAdmission container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OnlineAdmission';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OnlineAdmission container!',
  },
});
