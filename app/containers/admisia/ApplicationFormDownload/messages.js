/*
 * ApplicationFormDownload Messages
 *
 * This contains all the text for the ApplicationFormDownload container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ApplicationFormDownload';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApplicationFormDownload container!',
  },
});
