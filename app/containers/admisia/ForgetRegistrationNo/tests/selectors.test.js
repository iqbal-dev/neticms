// import { fromJS } from 'immutable';
// import { selectForgetRegistrationNoDomain } from '../selectors';

describe('selectForgetRegistrationNoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
