/*
 * ForgetRegistrationNo Messages
 *
 * This contains all the text for the ForgetRegistrationNo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ForgetRegistrationNo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ForgetRegistrationNo container!',
  },
});
