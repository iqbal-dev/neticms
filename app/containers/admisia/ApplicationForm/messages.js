/*
 * ApplicationForm Messages
 *
 * This contains all the text for the ApplicationForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ApplicationForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApplicationForm container!',
  },
});
