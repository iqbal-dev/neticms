// import { fromJS } from 'immutable';
// import { selectAdmissionConfirmationLetterDomain } from '../selectors';

describe('selectAdmissionConfirmationLetterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
