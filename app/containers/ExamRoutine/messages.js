/*
 * ExamRoutine Messages
 *
 * This contains all the text for the ExamRoutine container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExamRoutine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExamRoutine container!',
  },
});
