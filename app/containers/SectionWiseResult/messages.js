/*
 * SectionWiseResult Messages
 *
 * This contains all the text for the SectionWiseResult container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SectionWiseResult';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SectionWiseResult container!',
  },
});
