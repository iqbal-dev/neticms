/*
 * Awards Messages
 *
 * This contains all the text for the Awards container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Awards';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Awards container!',
  },
});
