/*
 * FindPayslip Messages
 *
 * This contains all the text for the FindPayslip container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FindPayslip';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FindPayslip container!',
  },
});
