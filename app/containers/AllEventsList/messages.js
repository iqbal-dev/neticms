/*
 * AllEventsList Messages
 *
 * This contains all the text for the AllEventsList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllEventsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllEventsList container!',
  },
});
